#include <stdio.h>
#define T "Tambor de hojalata.I"

int main(){

    char st[21] = "Todo puede hacerse.I";
    system("cls || clear");
    puts(T);
    puts ("Permiso para salir en la foto.");
    puts(st);
    puts (&st[8]);
    return 0;
}