/* INFORMATION::

EL último elemento de la estructura de datos es el primero que se saca 
1 <-Primero en insertar, por lo tanto, último en salir  
2
3
4
5 <-Ultimo en insertar, por lo tanto, primero en salir (TOPE)

Se le conoce como LIFO (Last-Input First-Output : El último en entrar es el primero en salir)

Requiere de arreglos y listas :)
push - insertar
pop - eliminar

op aux
pila_vacia
pila_llena 
*/

#include<stdio.h>
#include<stdlib.h>
enum Bool{FALSE, TRUE};

/* int pila_vacia(pila, tope){
    if(tope==0) it means que la pila está vacía
        si es asi, meter más datos
    else if(pila_llena) mandar error
        else push;
    }

    int pila_llena(pila,tope){
        if(tope==max) return 1; //está llena
        return 0;
    }

*/

int main(){

    return 0;
}