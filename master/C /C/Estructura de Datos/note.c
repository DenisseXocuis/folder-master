#include<stdio.h>

int main(int argc, char **argv){ //para indicar argumentos de llamada

/*
argc (argument count) = cantidad de argumentos que le hemos pasado a la función incluyendo el llamado de la función.

*argv[] or **argv (argument vector) = array apuntando a un espacio de memoria donde se encuentran los argumentos en tipo de dato string 

*envp (environment path) =  ruta del entorno
 */

    return 0;
}