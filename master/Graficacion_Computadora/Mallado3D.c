/* Hacer un mallado 3D con estructura de datos, lista de adyacencia */
#include <stdio.h>
#include <stdlib.h>
/* vertices determinadas por le usuario */

/* nodo vertice, meter coordenadas, generar triángulos de acuerdo a eso, imprimir cuantas caras se están formando en el mallado ;)*/
    typedef struct nodo_vertice
    {
        /* el nodo principal tendría las coordenadas y se tendría que verificar si hay conectividad con el que sigue*/
    }VERTICE;

    typedef struct lista_adyacencia
    {
        struct nodo_vertice *vertice;

    }LISTAA;

    typedef struct grafo
    {
        
    }GRAFO;